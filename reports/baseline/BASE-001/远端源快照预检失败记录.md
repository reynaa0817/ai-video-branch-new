# BASE-001 远端源快照预检失败记录

> 状态：**已触发规格纠偏，作为历史负向证据保留。** 本记录证明当时把本机 SHA 写成发布不变量是错误的，不再要求将该提交引入远端。

## 结论

2026-07-14 的只读 hermetic preflight 失败。Story 冻结的 `source_snapshot_sha`：

```text
7bc2f4561a9284728cb92b15b9ae9ee760abfa5c
```

无法从规范 remote `https://github.com/aif-go/ag-core.git` 恢复。按 AC1/AC4，BASE-001 必须失败关闭；不得用 `/Users/zhangyong/Downloads/ag-core` 或其他本机 checkout 替代远端证据。

## 执行证据

规范远端只读查询：

```text
REMOTE_HEAD=d19907276b3ee32eff34e1de41604c7a94c249d0
v0.0.1-alpha.2=0e1681eff7f3c18a8e490134747e214724df9208
v0.0.1-alpha.3=1624c77ab90b12b77191c38005b64d59f8a0029e
```

在全新临时 Git 仓库直接获取冻结 SHA：

```text
git fetch --depth=1 origin 7bc2f4561a9284728cb92b15b9ae9ee760abfa5c
fatal: remote error: upload-pack: not our ref 7bc2f4561a9284728cb92b15b9ae9ee760abfa5c
```

随后从规范远端执行全新 `--filter=blob:none --no-checkout` clone，共取得 56 个 refs；`git cat-file -e <sha>^{commit}` 仍失败：

```text
SOURCE_SNAPSHOT_REACHABLE=false
```

所有临时目录在命令退出时自动清理；未执行 push、tag 创建、ruleset 变更或任何远端写操作。

## 影响

- 无法满足 `tool_source_ref → tool_source_sha` 的远端可恢复前提。
- 证明“tool source SHA 必须等于本机调查快照”的正向路径设计错误，需要删除该不变量。
- 不允许签署真实 baseline manifest，不允许将 G0-1 更新为 Verified，不允许启动 Story 1.3 脚手架。
- fixture ATDD 的 13/13 仅证明本地门禁接口与错误分类，不是远端真实放行证据。

## 恢复条件

Correct Course 已批准以下恢复路径：

1. 从规范 remote 的可达提交/tag 中选择候选并执行只读 preflight；
2. 若现有候选无法满足合同，完成受审 ag-core PR 后使用 merge SHA 作为 tool source 候选；
3. clean-build/provenance 通过并取得 Architecture 批准后，再满足发布者权限、tag/ruleset 保护和 tool/root 双锁审批。

本机 SHA `7bc2f456…` 不再进入发布 manifest、schema 或 gate 断言。
