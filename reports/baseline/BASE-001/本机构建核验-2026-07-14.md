# BASE-001 本机构建核验（2026-07-14）

## 结论

用户已于 2026-07-14 11:16 在本机成功生成 ag-core 工具二进制。该结果证明当前本地 workspace 下源码可编译，但尚不能作为 BASE-001 的 hermetic/remote 放行证据。

## 已发现二进制

目录：`/Users/zhangyong/.gvm/pkgsets/go1.25.5/global/bin/`

- `aggo`
- `gen-go-db`
- `protoc-gen-go-agapi`
- `protoc-gen-go-aghertz`
- `protoc-gen-go-agkitex`
- `protoc-gen-go-agserver`
- `protoc-gen-go-agservice`

## 通过项

- 七个源码命令均产生了可执行文件。
- `path`/`mod` 使用 `github.com/aif-go/ag-core` 命名空间。
- 未发现旧 `gitlab.allinfinance.com/aifgo/ag-core` provenance。
- VCS revision 均记录为 `7bc2f4561a9284728cb92b15b9ae9ee760abfa5c`。

## BASE-001 未通过项

1. 数据库工具实际安装名为 `gen-go-db`，PATH 中没有契约要求的 `gendb`。
2. 七个二进制均为 `+dirty`，metadata 为 `vcs.modified=true`。
3. 五个 protoc 插件的 root dependency 显示为 `github.com/aif-go/ag-core (devel)`，没有可机器解析的 `root_dep_version`，说明构建受本地 workspace/root module 解析影响。
4. VCS revision 对应的提交不在规范远端可恢复，不能匹配待签署 manifest 的 `tool_source_ref/tool_source_sha`。
5. 构建产物位于开发机全局 GVM bin；缺少全新 checkout、隔离 cache、依赖下载和命令证据。

## 判定

本次结果应记录为“local workspace build succeeds”，修正此前“工具不能构建”的宽泛表述；但 AC2/AC3/AC4 仍未满足。下一步应复现用户实际构建方式，区分本地 go.work 提供了哪些依赖，再把同一能力迁移到规范远端的干净模块状态。
