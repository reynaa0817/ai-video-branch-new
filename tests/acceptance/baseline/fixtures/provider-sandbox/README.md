# Provider sandbox fixture 边界

这些 JSON 只为 Story 1.6 前的预算合同测试冻结最小快照形态。它们不是 Story 3.1 的管理员发布物，不得由 internal-prod 读取。历史 Attempt 必须保存并引用原始 snapshot/quote/policy/eligibility ID，不能跟随当前配置漂移。

本目录不实现 provider simulator、预算账本或三态提交。
