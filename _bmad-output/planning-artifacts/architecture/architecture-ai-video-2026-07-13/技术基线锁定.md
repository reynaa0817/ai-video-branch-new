---
tags:
  - ai-video
  - architecture
  - technology-lock
date: 2026-07-14
title: AI漫剧创作平台技术基线锁定
status: provisional
---

# AI 漫剧创作平台技术基线锁定

## ag-core

| 字段 | 当前值 | 放行状态 |
| --- | --- | --- |
| 规范 remote | `https://github.com/aif-go/ag-core.git` | 已确认 |
| Module | `github.com/aif-go/ag-core` | 已确认 |
| Go | `1.25.0` | 已确认 |
| 本机核验 checkout | `/Users/zhangyong/Downloads/ag-core` | 仅核验证据，不是规范路径 |
| 工作快照 SHA | `7bc2f4561a9284728cb92b15b9ae9ee760abfa5c` | 已核验 |
| 远端 immutable ref | 未发布 | **P0 阻塞** |
| CI 可恢复验证 | 未执行 | **P0 阻塞** |
| aggo/gendb/protoc 工具链 | 当前安装仍为旧 GitLab build | **P0 阻塞** |

## 工具链验收

必须从远端 immutable ref 重建：

- `tool/cmd/aggo`
- `tool/cmd/gen-go-db`（安装名 `gendb`）
- 全部 `tool/cmd/protoc-gen-go-ag*`

每个二进制的 `go version -m` 必须满足：

- `path`/`mod`/ag-core `dep` 只出现 `github.com/aif-go/ag-core`；
- 不出现 `gitlab.allinfinance.com/aifgo/ag-core`；
- 构建 commit/ref 与本文件一致。

## 其他 P0

| 技术 | 待锁定证据 |
| --- | --- |
| Temporal | Server/SDK patch、schema、MySQL 8.4、升级/回滚/恢复矩阵 |
| Kafka/agsarama | Broker minor/patch、生产/消费/rebalance/DLQ/KRaft 故障矩阵 |
| Nacos/Redis | 与 GitHub ag-core commit 的 server/SDK 兼容矩阵 |
| FFmpeg | 8.1.2 镜像 digest 与 H.264/AAC/libass/字体/标识金样 |
| Web | Node 镜像、React/Vite/TypeScript lockfile |
| Kubernetes | 1.35 发行版/patch、CNI/CSI/Ingress、恢复演练 |

本文件所有 P0 项关闭后，`status` 才能由 `provisional` 改为 `locked`。
