---
tags:
  - ai-video
  - architecture
  - technology-lock
date: 2026-07-14
title: AI漫剧创作平台技术基线锁定
status: provisional
---

# AI 漫剧创作平台技术基线锁定

## ag-core

| 字段 | 当前值 | 放行状态 |
| --- | --- | --- |
| 规范 remote | `https://github.com/aif-go/ag-core.git` | 已确认 |
| Module | `github.com/aif-go/ag-core` | 已确认 |
| Go | `1.25.1` | Story 1.1 已验证 |
| 历史调查 checkout/SHA | `/Users/zhangyong/Downloads/ag-core` @ `7bc2f4561a9284728cb92b15b9ae9ee760abfa5c` | 非规范证据，不参与放行 |
| Tool source ref/SHA | `3ad9bb9cf7106560400391b62a7f373f682cf591` | Story 1.1 已验证 |
| Root dep version/SHA | `v0.0.1-alpha.3` / `1624c77ab90b12b77191c38005b64d59f8a0029e` | Story 1.1 已验证 |
| 远端 immutable ref | Tool 使用内容寻址 commit；root tag 由 ruleset `18908015` 禁止更新/删除 | Story 1.1 已验证 |
| CI 可恢复验证 | 权威脚本已从空 checkout/cache 通过；GitHub Actions job 已固化，ai-video 尚无 remote，未宣称远端 CI run | Story 1.1 已验证 |
| aggo/gendb/protoc 工具链 | Go 1.25.1 隔离重建 7/7；GitHub provenance、VCS revision/clean 状态通过 | Story 1.1 已验证 |

## 工具链验收

必须从远端 immutable ref 重建：

- `tool/cmd/aggo`
- `tool/cmd/gen-go-db`（安装名 `gendb`）
- 全部 `tool/cmd/protoc-gen-go-ag*`

每个二进制的 `go version -m` 必须满足：

- `path`/`mod`/ag-core `dep` 只出现 `github.com/aif-go/ag-core`；
- 不出现 `gitlab.allinfinance.com/aifgo/ag-core`；
- 构建 commit/ref 与本文件一致。

## 其他 P0

**责任与放行：** ag-core 与生成工具由 Story 1.1 关闭；其余全栈 P0 由 Story 1.2 关闭。Story 1.3 的完整脚手架与 internal-prod 放行必须同时取得两项 Story 的可恢复证据。

Story 1.1 证据位于 `reports/baseline/BASE-001/`。失败时禁止移动已登记 ref；恢复到上一条已验证 manifest，保留失败日志和候选 SHA。本文件整体仍为 `provisional`，其余 P0 由 Story 1.2 继续关闭。

| 技术 | 当前候选/证据 | 放行状态 |
| --- | --- | --- |
| Temporal | Server `1.31.2`、Go SDK `1.45.0`、schema `1.19/1.14`、MySQL `8.4.10`；SDK `1.46.0` 因要求 Go 1.25.4 被拒绝 | 等待升降级与恢复实测 |
| Kafka/agsarama | Kafka `4.0.0`、Sarama `1.46.3`；三节点 RF3/minISR2、acks=all、DLQ、leader 故障 smoke | 等待双 consumer rebalance |
| Nacos/Redis | Nacos `3.2.2`、Redis `8.8.0` 固定 digest 容器 smoke | 等待 ag-core SDK、配置快照与升级矩阵 |
| 对象存储 | MinIO `RELEASE.2025-04-22T22-12-26Z` integration checksum/versioning smoke | integration 已验证；internal-prod 产品仍 Open |
| FFmpeg | 项目镜像 `8.1.2-noto-serif-cjk-2.003`，媒体金样与 ffprobe 证据 | integration 已验证 |
| Web | Node `24.18.0`、pnpm `11.4.0`、React `19.2.7`、Vite `8.1.4`、TypeScript `5.9.3` | 双隔离 frozen build 与负向检查已验证 |
| Kubernetes | kind `1.35.0`、CNI `1.9.1`、hostpath CSI `1.17.0`、Ingress `1.15.1` | 静态 harness 已验证；集群/回滚/恢复实测待完成 |

Story 1.2 当前证据位于 `reports/baseline/BASE-002/`。G0-2 继续保持 `Open`，不得放行 Story 1.3。

本文件所有 P0 项关闭后，`status` 才能由 `provisional` 改为 `locked`。
