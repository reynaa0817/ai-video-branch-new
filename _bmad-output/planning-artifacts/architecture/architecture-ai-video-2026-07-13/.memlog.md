---
scope: AI 漫剧创作平台从 idea 到内部验证版本的端到端生产、质量、预算、治理与运营控制
purpose: 为内部产品、架构、研发和测试提供平台级一致性契约，并支撑后续 Epic 拆分与实施
altitude: initiative
updated: 2026-07-14T01:22
---

- (decision) 工作模式采用 Coaching path；所有承重架构选择先展示现实替代方案、权衡与推荐，再由用户确认。
- (decision) 主交付为 build-substrate 型 ARCHITECTURE-SPINE.md；同时产出面向内部产品、架构、研发和测试的系统架构说明与 C4、生产时序、服务边界、部署视图。
- (constraint) 继承已定稿 PRD 和 UX：桌面优先 Web、普通创作者隐藏模型参数、管理员配置模型节点、六阶段体验、五类常规确认闸门、事件型阻断、MVP 仅内部验证版本和受控导出。
- (constraint) 架构必须共同优化且可观测三个成败指标：视频质量、生成效率、成本控制；任何回退不得静默突破完整动态、锁定设定或预算上限。
- (direction) 宏观范式候选：A 模块化单体控制面 + 持久化工作流编排 + 无状态执行面 worker（推荐）；B 从首版开始事件驱动微服务；C 低代码/节点图或 Serverless 函数直接串联模型；D 单体应用 + 普通任务队列/定时器。推荐 A，因其同时保留早期交付速度、明确数据所有权、长流程恢复与未来按 worker 维度水平扩展能力。
- (direction) 推荐范式的核心边界：控制面是项目、确认、预算账本、配置快照、质量证据和审计的唯一事实源；执行面 worker 只执行幂等任务尝试并返回不可变产物/证据，不得直接推进项目阶段、修改预算或覆盖已采用版本。媒体二进制进入对象存储，事务数据库只保存元数据、关系和状态。
- (decision) 用户明确选择微服务作为首版部署范式，否决模块化单体控制面；该选择为 [ADOPTED]，后续不再重新比较单体与微服务。
- (direction) 调整后的推荐范式：按限界上下文拆分的事件驱动微服务 + 持久化工作流编排 + 控制面/执行面分离。服务按业务事实与数据所有权划分，不按六个 UX 阶段、单个模型或工作流节点拆分；每个服务独占写模型，禁止共享数据库写入。
- (direction) 微服务不变量候选：预算只能由预算账本服务预留与结算；项目阶段和确认只能由作品域服务接受业务事实后推进；工作流编排只拥有流程执行状态，不成为项目、预算或资产采用版本的事实源；执行 Worker 只返回任务尝试与不可变产物。
- (decision) 微服务拆分的优先驱动力由用户确认：[ADOPTED] 业务边界清晰、故障隔离、独立扩缩容；不以组织人数、形式上的服务数量或按 UX 阶段一一映射为目标。
- (direction) 首版核心业务服务候选：Studio 作品域、Workflow 生产编排域、Budget 预算账本域、Asset 媒体资产域、Model Gateway 模型接入域、Quality 质量与连续性域、Delivery 内部版本与交付域；API Gateway/BFF、身份、通知、可观测与分析投影属于平台支撑，不拥有核心业务真相。
- (direction) 执行面候选：文本/图片/视频/声音/后期处理 Worker 池按资源类型与供应商故障域独立部署扩缩；Worker 不等同新的业务限界上下文，统一通过 Model Gateway/任务协议接入并返回任务尝试、产物引用、成本和质量原始证据。
- (direction) 跨服务一致性候选：用户命令同步进入事实拥有服务；跨域推进使用领域事件与持久化 Saga/工作流；每个写服务使用 transactional outbox 与 inbox/idempotency，禁止分布式事务和跨库写入。
- (decision) 用户确认 [ADOPTED] 七个核心服务边界：Studio、Workflow、Budget、Asset、Model Gateway、Quality、Delivery；平台支撑与执行 Worker 不作为新的核心业务事实源。
- (direction) 跨服务生产一致性候选采用 orchestration-first Saga：Workflow 负责推进长事务与补偿，不采用纯事件 choreography 或 2PC；业务事实仍由各领域服务独占，Workflow 只保存流程游标、等待信号和活动结果引用。
- (direction) 每个付费尝试候选协议：Budget 先按最大计费责任原子预留；预留成功后 Model Gateway 才可提交供应商；供应商结果和收费事实独立结算；Asset 登记不可变候选与血缘；Quality 对候选运行检查；只有采用命令与闸门确认可改变当前采用版本或作品阶段。
- (direction) 未知供应商结果候选规则：提交已被供应商接受后发生超时，不能视为失败或立即释放预算；尝试进入 UNKNOWN_RECONCILING，保持预留并查询/对账。输出不可用仍需按供应商收费事实结算，重试必须创建新的 attempt 与预算预留。Budget 不可用时付费任务 fail-closed。
- (direction) 消息语义候选：at-least-once + 消费端幂等；所有写服务使用 outbox；事件携带 event_id、schema_version、aggregate_id/version、correlation_id、causation_id、idempotency_key、occurred_at UTC 与 trace_id。浏览器不能直接唤醒 Workflow，用户确认必须先由 Studio 持久化再以领域事件/受信信号推进。
- (decision) 用户确认 [ADOPTED] Workflow 编排的长事务 Saga：预算预留后提交、供应商收费事实独立结算、不可变候选入库、质量检查后仍需明确采用、Studio 持久化确认后再推进工作流；未知供应商结果保持预留并进入对账。
- (direction) 身份与版本候选：所有跨服务实体使用全局唯一不透明 ID；每个聚合有单调 aggregate_version 做乐观并发；服务间只传 ID 与不可变快照引用，不建跨服务外键。作品阶段状态、内容 revision、资产 slot 当前采用指针、供应商 attempt 分离建模。
- (direction) 从首版引入 workspace_id 作为数据隔离键，内部阶段只有默认 workspace；不承诺完整多租户计费与管理，但所有核心聚合、对象存储路径、事件与授权上下文都携带 workspace_id，避免商业化时迁移主键和隔离边界。
- (direction) 资产候选：AssetSlot 表达作品中的稳定语义位置；ArtifactVersion 不可变并记录 URI/checksum/媒体规格/lineage/logical_task_id/attempt_id/model_config_snapshot_id；采用是独立 AdoptionRecord/当前指针，生成或质量通过不自动覆盖。
- (direction) ReleaseVersion 候选为不可变交付清单：精确引用创作快照、采用的 artifact versions、QG evaluation set、权利声明、AI 标识策略、成本快照与模型配置快照；确认后不可修改，后续修改创建新 release version，导出为独立 ExportRecord。
- (direction) 状态机候选避免组合状态爆炸：Project 分离 phase(创意/故事/视觉/制作/后期/成片) 与 lifecycle(ACTIVE/AWAITING_CONFIRMATION/PAUSED/BLOCKED/COMPLETED/ABANDONED)；Gate、WorkflowRun、Attempt、Reservation、QualityRun、ReleaseVersion 各自维护独立状态机，其他服务不得写入。
- (direction) 持久化模型候选不采用全局 Event Sourcing：各服务使用普通关系型聚合当前状态 + 不可变 revision/ledger/audit 表 + outbox。Budget ledger、ArtifactVersion、GateApproval、Quality evidence、ReleaseVersion 和审计记录追加写；可重建投影不成为事实源。
- (decision) 用户确认 [ADOPTED] 身份、版本与状态模型：关系型聚合当前状态 + 不可变 revision/ledger/evidence/version + 明确 AdoptionRecord；不采用全局 Event Sourcing。
- (decision) 用户确认 [ADOPTED] Project phase 与 lifecycle 分离；ReleaseVersion 不可变且确认与导出分离；workspace_id 从首版进入核心聚合与事件上下文。
- (event) 准备进入基础技术栈选择；按 Architecture 工作流要求，绑定任何具体技术和版本前先进行实时官方资料验证。
- (decision by user) 后端技术栈由用户指定为 ag-skills 明确的 ag-core，作为硬约束，不再参与通用框架选型。
- (decision by user) 七个核心业务服务均按 ag-core 微服务规范实现：Proto-first、aggo 生成适配层、Uber fx 依赖注入；业务逻辑位于 internal/biz，internal/service 仅做薄委托；禁止修改 api、internal/adpgen、internal/svcgen 生成代码。
- (decision by user) Web 公网入口集中在 API Gateway/Web BFF，使用 ag-core Hertz 暴露 HTTP；核心服务内部同步调用默认使用 Kitex/gRPC，不要求每个领域服务暴露公网 HTTP。
- (decision by user) 跨服务依赖遵循 biz 定义 Gateway 接口、gateway 完成协议适配、clients 管理原生客户端；生产通过 Nacos 服务发现，本地开发支持 direct 直连。
- (decision by user) 异步领域事件与 Outbox relay 使用 ag-core agsarama/Kafka 能力；消费语义保持 at-least-once，业务消费者必须使用 Inbox/幂等键，关键失败落库或死信，不能只依赖 offset 重试。
- (decision by user) 服务配置使用 AgConf 三件套与 Nacos 远程配置，结构化日志使用 AgLog；缓存采用 agredis 时仅作为派生加速层，不承担预算、项目阶段或工作流状态等业务事实。
- (decision by user) 关系数据访问采用 ag-core agdb/gendb DAO 规范；当前生成链原生支持 MySQL/DB2，因此默认数据库候选收敛为 MySQL，最终版本和部署形态仍待实时验证。
- (note by coach) 归因更正：上一组中仅“后端必须采用 ag-core”是用户直接决定；其余六条是依据 ag-skills 推导并提交确认的架构约束。
- (evidence by coach) 2026-07-14 官方核验：Temporal Go SDK覆盖 Workflows、Activities、Workers、消息传递、Timers、异步 Activity 完成、版本与可观测性，满足长流程、人工确认、暂停恢复和任务重试。
- (proposal by coach) 持久化工作流采用 Temporal；Workflow ag-core 服务负责启动、Signal、Query 与管理 API，Temporal Workflow 只做确定性编排，所有跨服务和供应商调用放在 Activities。
- (evidence by coach) Temporal 自托管当前支持 MySQL 8.0.17+ 的 Advanced Visibility；首版可用 MySQL 同时承载 Temporal persistence 与 visibility 的独立数据库，无需立即引入 Elasticsearch/OpenSearch。
- (proposal by coach) 业务数据库采用 MySQL 8.4 LTS；七个服务逻辑独占 database/schema 与账号，内部验证期可共用一套高可用集群以控制成本，禁止跨服务 SQL 与外键。
- (evidence by coach) Apache Kafka 官方当前支持 4.3.1、4.2.1、4.1.2；ag-core 使用 IBM Sarama 封装，精确 Kafka 版本需通过 agsarama 协议和故障恢复契约测试后锁定。
- (proposal by coach) Kafka 保持 agsarama 接入，生产使用 KRaft 三节点；版本采用受支持的 4.x 分支但不在兼容性测试前盲目绑定最新 patch。
- (evidence by coach) MinIO 开源仓库于 2026-04-25 归档且明确不再维护，社区版仅源码分发，不能作为本项目生产默认对象存储。
- (proposal by coach) 对象存储固定 S3 兼容端口，生产选择托管对象存储，开发使用文件系统适配或真实测试桶；媒体二进制不进入 MySQL、Kafka 或 Temporal history。
- (evidence by coach) React 官方当前版本 19.2，Vite 当前版本 8.1.4；产品是登录后的重交互创作台且已有 Go BFF，不需要为 SSR 再引入 Next.js 服务端。
- (proposal by coach) Web 端采用 React 19 + TypeScript + Vite 8 SPA；服务端状态与本地编辑状态分离，最终媒体合成不在浏览器完成。
- (evidence by coach) OpenTelemetry Go 当前 traces 与 metrics 稳定、logs 为 beta；Collector 官方建议生产环境承担批处理、重试、加密和敏感字段过滤。
- (proposal by coach) 可观测性采用 OTel traces/metrics + Collector，AgLog 输出 JSON 日志；后端使用 Prometheus、Tempo、Loki、Grafana，统一 workspace/project/workflow/attempt/trace 关联。
- (evidence by coach) Kubernetes 当前维护 1.36、1.35、1.34；FFmpeg 当前稳定版本为 8.1.2。
- (proposal by coach) 生产部署使用 Kubernetes 受支持的 n-1 minor，开发用 Docker Compose；后期处理 Worker 固定 FFmpeg 稳定分支并通过镜像锁版本。
- (risk by coach) Seedance 2.0 官方确认文字、图片、音频、视频四模态输入和原生音画能力，但本次未找到可公开审计的 API 合约；API 开通、模型 ID、配额、回调、幂等、取消和计费语义必须作为实现前 P0 验证。
- (decision by user) 用户确认整体采用推荐技术栈：Temporal 自托管、MySQL 8.4 LTS、agsarama/Kafka、AgConf/Nacos、agredis、S3 兼容托管对象存储、React+TypeScript+Vite SPA、FFmpeg、Docker Compose/Kubernetes、OpenTelemetry+Grafana 栈。Kafka/Nacos/Redis 精确版本以 ag-core 兼容性测试后锁定。
- (proposal by coach) 部署映射提案：Temporal 是唯一生产流程推进器；Kafka 只传播各领域已持久化的事实。Workflow bounded context 由 workflow-api、workflow-worker、workflow-signal-bridge 三种进程角色组成，独立部署但共享同一领域模型，不拆成三个微服务。
- (proposal by coach) Model Gateway 由内部 gRPC API、受限 provider callback HTTP ingress、按 provider/media type 独立扩缩的 executor/poller workers 组成；回调先验签并持久化 ModelTask，再由 outbox 发布事实，禁止回调直接 Signal Temporal 或修改预算/项目。
- (proposal by coach) Temporal Workflow history 只保存 ID、版本、状态与不可变引用，不保存剧本全文或媒体；短服务调用放 Activity，小时级等待使用 Signal/Timer；达到历史阈值或跨集续作时 Continue-As-New。
- (proposal by coach) 生产入口分为用户 Web BFF 与供应商 Callback Ingress 两条受控路径；内部服务默认 Kitex/gRPC+Nacos，事件使用 Kafka；不在首版引入 service mesh。
- (decision by user) 用户确认 Temporal 单一编排主线与 Kafka 事实事件职责：Temporal 唯一推进生产流程；Kafka 只传播已持久化事实；Workflow 和 Model Gateway 采用同一限界上下文内多进程角色；供应商回调先持久化再经事件桥唤醒流程；首版不引入 Service Mesh。
- (proposal by coach) 代码组织提案：内部验证期采用单 Git monorepo，但 web-bff 与七个核心服务各自是完整 ag-core 独立 Go Module、镜像和部署单元；go.work 仅本地联调，CI 必须脱离 go.work 独立构建。
- (proposal by coach) 跨服务禁止 import 其他服务 internal/biz/repository/model/dao；同步仅依赖版本化 Proto/API，异步仅依赖版本化事件契约；shared 只含 ID、事件 Envelope、trace/context、错误基础和测试工具，禁止共享业务实体。
- (decision by user) 用户确认采用单 Git Monorepo + 每服务独立 ag-core Go Module、镜像和部署单元；go.work 仅用于本地联调，CI 独立构建；禁止跨服务共享业务代码，未来可沿 Module 边界拆仓。
- (proposal by coach) 契约提案：同步 API 与领域事件都使用 Proto 定义；Kafka 首版使用 protojson JSON wire format，不立即引入 Schema Registry。服务独占自身 API/Event schema，v1 仅做兼容新增，破坏性变更发布并行 v2。
- (proposal by coach) Topic 默认按 bounded context 为 ai-video.<env>.<domain>.events.v1，message key=aggregate_id；事件只表达过去事实并携带统一 Envelope，不传媒体与大文本。消费者按 aggregate_version 检测重复、乱序和缺口。
- (proposal by coach) 可靠消息提案：状态与 Outbox 同一 MySQL 事务；消费端以 event_id+consumer_name 建 Inbox 唯一约束，Inbox 与业务更新同事务；失败先持久化或进入 DLQ 再推进 offset，重放走同一幂等路径。
- (decision by user) 用户确认 API/Event 均由 Proto 定义、Kafka 首版使用 protojson、按领域 Topic、Outbox/Inbox 可靠消息方案。
- (constraint by user) Kafka 必须使用 ag-core 内置 agsarama 封装；业务代码不得自行创建原生 Sarama Client。配置、fx 注入、producer 生命周期与 consumer ag_server.Server 注册遵循 ag-core 规范，兼容性测试针对 agsarama 完整链路。
- (proposal by coach) 安全提案：用户认证固定 OIDC/OAuth 2.1 协议但暂不绑定 IdP；浏览器仅持 Secure HttpOnly SameSite Session Cookie，token 留在 BFF 服务端，禁止 LocalStorage。
- (proposal by coach) 内部 Kitex 调用必须携带短期签名服务/委托身份；ag-core middleware 验证签名、audience 和过期时间。workspace_id 来源于可信身份和成员关系，领域服务执行资源级授权，BFF 只做粗粒度权限。
- (proposal by coach) 供应商与基础设施凭证进入 Secret Manager/外部密钥机制，不进入 Git、普通 app.yml、Nacos 明文和日志；媒体使用短期预签名 URL；回调验签、防重放、限流并审计。
- (decision by user) 用户确认 OIDC + BFF 服务端 Session + 内部短期签名身份的安全边界；领域服务执行资源级授权，workspace_id 来自可信身份，密钥进入 Secret Manager，私有媒体使用短期预签名 URL。
- (proposal by coach) 环境提案：local Compose、共享 K8s integration namespace、隔离的 internal-prod namespace；内部生产可暂与集成共享物理集群但不得共享数据库/topic/Temporal namespace/bucket/secret。对外发布时新建独立生产集群，不把验证环境原地公网化。
- (proposal by coach) 内部生产基线：核心 API 2 replicas；MySQL HA+binlog/PITR；Kafka 3-node KRaft RF3 minISR2；Nacos 3-node+external MySQL；对象存储版本/加密/跨故障域；内部阶段 RPO<=5m、RTO<=2h。
- (proposal by coach) 降级规则：Temporal/Kafka 故障暂停跨域生产但保留本地事实；Budget 和供应商配额限流 fail-closed；对象未持久化不算资产完成；Quality 不可用不得跳过 QG；通知/分析故障不阻断主流程。
- (proposal by coach) 恢复规则：MySQL 快照+PITR并定期恢复演练；Kafka 不是事实备份；对象版本与数据库引用/校验和对齐；ReleaseVersion、Ledger、ArtifactVersion、Quality Evidence 和 audit 追加保留。
- (decision by user) 用户确认内部验证期环境与容灾基线：local Compose、隔离的 integration/internal-prod、核心 API 双副本、MySQL/Kafka/Nacos 高可用、RPO<=5m/RTO<=2h、按故障矩阵 fail-closed/可恢复；对外发布新建独立生产集群。
- (proposal by coach) Model Gateway 提案：所有模型唯一出口；管理员 ModelProfile 生成不可变 ModelConfigSnapshot，运行中任务锁定 snapshot，配置变更只影响新任务。Studio/Workflow/Quality/Delivery 禁止直接调用供应商或读取凭证。
- (proposal by coach) 提交受理必须三态 NOT_ACCEPTED/ACCEPTED/UNKNOWN；UNKNOWN 进入对账并保留预算，禁止自动重放 POST。备用模型只在 NOT_ACCEPTED 且策略允许时以新 attempt+reservation+snapshot 切换。
- (proposal by coach) ModelTask 只有结果已转存平台对象存储、校验 checksum 并产生候选引用后才 COMPLETED；供应商成功但转存失败停在 TRANSFERRING 重试，不能重新生成。
- (proposal by coach) Provider executors 按 provider/capability/region 独立扩缩；并发受供应商配额、平台上限、workspace 预算和优先级约束。Redis 令牌桶故障时付费提交 fail-closed，权威任务/租约仍在 MySQL。
- (decision by user) 用户确认 Model Gateway 标准任务协议：唯一模型出口、不可变配置快照、NOT_ACCEPTED/ACCEPTED/UNKNOWN 三态、结果转存后才完成、Seedance 不因短时延迟自动切换、并发与配额 fail-closed。
- (proposal by coach) Quality 提案：Quality 独占版本化 RuleSet、QualityRun、Finding、Evidence、ReviewDecision，但不能修改创作宪法、连续性账本、资产采用、项目阶段、预算或 Release；修复回到事实拥有服务并创建新版本。
- (proposal by coach) 质量不能压成总分：确定性技术检查可自动阻断；AI 语义检查只形成候选 finding，语义 blocker 由指定人工角色复核；创作者 QG-3 低于3阻断，3分仅本人接受。付费质量检查也入 Budget Ledger。
- (proposal by coach) QualityRun 固定 creative/constitution/continuity/artifact/manifest/rules/evaluator versions；任何直接引用变化使 run INVALIDATED，重检创建新 run，不覆盖历史。镜头级未受影响结果可复用，作品级 QG 对新 manifest 重跑。
- (proposal by coach) Release 准入必须在同一候选 manifest 上持有有效 QG1-6、零 blocker、warning 由正确角色接受；质量通过不自动采用资产或创建 Release，必须经 Workflow 和用户确认显式推进。
- (decision by user) 用户确认质量门与连续性闭环：Quality 只拥有规则/运行/证据/判定，不能修改业务事实；QG1-6 分开判定，AI语义问题人工复核，输入变化使 Run 失效，Release 需同一 manifest 上有效全量 QG。
- (proposal by coach) 预算提案：金额用最小货币单位/定点 Decimal 禁止 float；首版项目币种 CNY，外币成本保存原币+FX snapshot+折算。Project 锁定 CostPolicySnapshot，分开 user_billable 与 platform_cost。
- (proposal by coach) Model Gateway 用不可变 pricing/model snapshot 生成可验证 PricingQuote；Budget 只接受匹配且未过期 quote。available=limit-settled-active max liability，Reservation 以 attempt_id 幂等并在单 MySQL 事务原子条件更新。
- (proposal by coach) ACCEPTED 后 reservation 不得普通 TTL 过期；UNKNOWN/取消未知/账单未定进入 HELD_RECONCILING。Model Gateway ProviderCostFact 驱动结算，超最大责任部分记 platform_variance，不转嫁用户。
- (proposal by coach) 预算调整创建不可变 BudgetLimitRevision；降低不得小于 settled+reserved；退款/补贴/冲正只追加 LedgerEntry。所有金额可从 project 下钻到 logical task/attempt/quote/reservation/cost fact/ledger。
- (decision by user) 用户确认严格预算账本：定点金额/CNY+FX快照、可验证 Quote、原子预留、Accepted 不自动过期、未知计费 held、供应商超扣平台承担、追加式调整与可下钻审计。
- (proposal by coach) 资产提案：对象存储逻辑分 staging/assets/releases/evidence；其他服务只获短期 staging 上传授权，Asset 唯一负责校验、登记和 canonical key，AVAILABLE 后 ArtifactVersion 不可变。
- (proposal by coach) AssetSlot 是稳定语义位置，多候选单采用；AdoptionRecord 用 expected_current_version 乐观并发并记录 from/to/actor/reason。Quality pass 不自动采用，已被 Gate/Quality/Release 引用版本不可覆盖或级联删除。
- (proposal by coach) 所有字幕/混音/转码/缩略图/封装都是引用父版本和 recipe/tool snapshot 的新 ArtifactVersion。跨 workspace 禁止 checksum 自动复用；删除先 Tombstone、引用和保留检查。
- (proposal by coach) Delivery 从精确 adopted versions 生成 ReleaseCandidateManifest，经有效 QG1-6 与用户确认创建不可变 ReleaseVersion；ExportRecord 独立，按 Render/Export snapshot 幂等生成文件，不修改 Release 或采用指针；MVP 仅内部导出。
- (assumption) [ASSUMPTION] 用户要求不再逐项确认并直接完成架构，因此将此前尚未显式确认但与已采纳版本/质量/预算不变量一致的 Asset 与 Delivery 提案纳入最终架构；如后续变更，保持 AD ID 稳定并走架构更新。
- (decision) 资产与交付边界采用：Asset 唯一登记 canonical ArtifactVersion 与采用关系；所有衍生产物均新建不可变版本；Delivery 仅从精确 Manifest 和有效质量证据创建不可变 ReleaseVersion，ExportRecord 与 Release 分离。
- (decision) 平台范式最终定为按限界上下文拆分的事件驱动微服务 + 持久化 Workflow Orchestration + 控制面/执行面分离；Temporal 是唯一流程推进器，Kafka 只传播已持久化业务事实。
- (decision) 治理与删除采用可验证删除 Saga：Studio 接收删除意图，Asset 负责平台对象 tombstone/保留检查，Model Gateway 负责供应商删除请求与结果，Audit 保留非敏感证明；法定保留、Release 引用与调查保全优先于物理删除。
- (decision) 指标投影采用事件驱动只读投影，不成为业务事实源；质量、效率、成本均按 workspace/project/workflow/logical_task/attempt/release 可关联，失败与放弃项目也进入全量分母。
- (question) Seedance 2.0 API 的模型 ID、配额、回调、幂等、取消、计费与删除能力仍需供应商沙箱 P0 验证；未完成前不得进入真实付费生产。
- (question) ag-core 与 Kafka、Nacos、Redis 的精确生产版本待全链路兼容性和故障恢复测试锁定；架构只绑定适配接口与语义，不臆测未经验证的 patch 版本。
- (decision) 付费自然语言动作采用两阶段授权：AI 只能创建不可变 ChangeProposal/ImpactPlan，显式确认且版本/Quote 未过期后才能由 Workflow 创建 Reservation 与 Attempt。
- (decision) Web 工作台读取统一 ProjectExperienceView 投影；投影合并领域事实并提供单调版本与陈旧标记，BFF/Web 不自行推导业务状态，实时更新采用可恢复游标。
- (decision) 跨内容依赖使用版本化 DependencyManifest；上游变化按依赖图传播 stale，复用要求输入、配置、规则、工具与已锁定结果相同；局部重做通过 AdoptionManifest 原子切换成组媒体。
- (decision) Studio 固定五类常规 Gate 类型与前置 Manifest；高成本下游只能消费已批准 Gate 事实，批准与 Project 阶段转换在 Studio 内原子持久化后再 Signal Workflow。
- (decision) 内部导出采用版本化 DeliveryProfile：9:16、1080x1920、30fps、MP4/H.264、AAC 48kHz、字幕烧录与独立文件、可见 AI 标识与可验证元数据；不合规输入必须产生新转码版本。
- (decision) 敏感数据采用 DataPolicySnapshot 和 ProviderEligibilityPolicy；Model Gateway 提交前校验供应商条款与能力，平台敏感原始素材删除目标 30 天，内部审计记录最低保留 180 天，并记录例外与法定保全。
- (decision) 模型回退由版本化 FallbackPolicy 与 CapabilityContract 驱动，需证明输入输出兼容及完整动态/连续性底线；否则暂停并说明，不能仅因延迟自动切换。
- (decision) 指标口径使用不可变 CohortMembership、ComplexitySnapshot 与 MetricPolicySnapshot；前20部校准、按复杂度 p75、第21部前冻结、单层少于5部不宣称校准，历史不回写。
- (decision) 暂停区分 user/budget/blocked/stalled；只停止新调度，已 ACCEPTED/UNKNOWN 的 Attempt 继续对账和结算，恢复从持久化 Workflow 游标继续。
- (decision) 验收门固定控制面月可用性99.5%、非生成 Web p95不超过2秒、WCAG 2.1 AA核心要求；以 SLI、压测预算和自动化可访问性检查进入 CI/发布门。
- (decision) 质量 Warning 接受权限由版本化 QualityRolePolicy 固定：QG-3 仅创作者本人，QG-4 创作者与内部审核双接受，QG-6 不可覆盖；actor/scope/reason/time 进入证据。
- (decision) MVP 叙事节奏作为 Studio 拥有的不可变 NarrativePolicySnapshot，并由 Quality 确定性规则核验；生成约束与验收引用同一快照。
- (decision) 任务中心与站内提醒由事件驱动的 Notification Projection 构建，包含 read/ack/dedupe 状态；至少一次事件通过 event_id+recipient 去重，不依赖浏览器在场。
- (decision) 唯一事实所有权表固定所有核心聚合；Model Gateway 独占 Attempt/ModelTask，Budget 独占 Reservation/SpendAuthorization/Ledger，Workflow 只持 LogicalTask 与外部引用。
- (decision) 付费提交顺序固定为 AttemptIntent→Model Gateway 创建 PENDING_AUTHORIZATION Attempt→Budget 原子预留并签发单次 SpendAuthorization→Model Gateway 原子消费授权后提交供应商。
- (decision) 所有外部业务事实只经 workflow-signal-bridge 进入 Temporal：先以 event_id+workflow_id 持久化 WorkflowInbox，再发送同 dedupe key 的 Signal；领域服务禁止直接 Signal。
- (decision) 跨域 Manifest 统一使用不可变 ManifestRef 和确定性摘要；contracts/manifest/v1 固定 owner、schema、规范化、排序、闭包与 golden vector；同一 Manifest 要求 id 与 digest 同时相等。
- (decision) Release 采用 PrepareRelease Saga：Asset/Quality/Studio 对同一 ManifestRef 签发短期 attestation，Studio 签发 FinalReleaseConsent/ReleaseAuthorization，Delivery 验证同一 digest、expected versions 和有效期后创建 Release。
- (decision) Experience Projection 是 ProjectExperienceView 与 Notification Projection 的唯一部署 owner；Studio 独占 InteractionReceipt，投影用源水位与 canonical DecisionTaskRef 重建。
- (decision) 跨服务金额统一 Money Proto(currency_code,minor_units,currency_exponent) 与版本化舍入策略；AssetRegistrationReceipt、ExpectedAdoptionVector、ManifestRef 和授权凭证均由共享 contracts 定义并以 golden vectors 验证。
- (decision) internal-prod Temporal 必须多副本、独立 persistence/visibility schema 与账号、环境独立 Namespace、备份恢复和滚动升级/回滚演练；无演练证据不得放行。
- (decision) 跨服务演进采用 expand/migrate/contract 与向后兼容窗口；Proto/Event/DB/Temporal Workflow 均先部署兼容读写再迁移，镜像/配置可回滚，破坏性变化并行版本。
- (decision) 技术栈状态修正为目标基线而非已物化构建；ag-core 必须锁唯一 GitLab module/tag/SHA，OTel 是应用层集成，Kafka/Temporal/FFmpeg/K8s/前端均在 P0 证据通过前禁止 internal-prod。
- (decision) QG-2 轻微提醒由创作者与内部审核共同接受；锁定设定 Blocker 不可覆盖，必须修改创作宪法后重检。
- (decision) Studio 独占 ChangeProposal、ImpactPlan 与 ProposalConfirmation；确认作为已持久化事实发布，经 workflow-signal-bridge 唯一进入 Temporal。
- (decision) SpendAuthorization 的单次消费权威留在 Budget：Model Gateway 提交前命令 Budget 原子从 ISSUED 转 CONSUMED 并取得 SpendAuthorizationReceipt；Model Gateway 持久化回执后才提交供应商。
- (decision) SpendAuthorization 相同 authorization_id+attempt_id+nonce 的消费重试必须返回同一不可变 Receipt；只有绑定不一致、过期未消费或被不同幂等键消费时拒绝。
- (event) spine finalized; lint 0; rubric PASS; technology reality PASS; adversarial compatibility PASS; Obsidian record archived
- (constraint) 归因修正：ai-video 的 ag-core 权威来源是本地仓库 /Users/zhangyong/Desktop/ag-core；module gitlab.allinfinance.com/aifgo/ag-core 仅是 Go import/module 标识，Git remote 仅是本地仓库上游元数据，不得当作构建来源。
- (decision) ag-core 构建基线锁定本地仓库当前 commit f72e9f23536b03c363017a3891ef837560afa199；本地开发通过 go.work/use 或等价工作区解析，CI 使用同一 commit 的本地 checkout/build context，禁止回退到 github.com/aif-go/ag-core 或从 GitHub 拉取替代实现。
- (event) corrected ag-core provenance: local /Users/zhangyong/Desktop/ag-core at f72e9f23536b03c363017a3891ef837560afa199 is authoritative; remote/module names are identifiers only; GitHub implementation excluded
- (constraint) 用户最终澄清：ai-video 必须采用 GitHub 版 ag-core，即本地 checkout /Users/zhangyong/Downloads/ag-core，module github.com/aif-go/ag-core；排除 /Users/zhangyong/Desktop/ag-core 的 GitLab 版。
- (evidence) 当前安装工具链并非 GitHub 版：/Users/zhangyong/.agents/skills/ag-core/bin/aggo build metadata 为 gitlab.allinfinance.com/aifgo/ag-core/tool/cmd/aggo v0.9.24+dirty，gendb 为 GitLab module v0.9.28+dirty；GitHub 本地仓已提供 github.com/aif-go/ag-core/tool/cmd/* 源码。
- (decision) ag-core 架构基线锁定 /Users/zhangyong/Downloads/ag-core commit 7bc2f4561a9284728cb92b15b9ae9ee760abfa5c；实施前必须从该 commit 重建 aggo 与 protoc-gen-go-ag* 工具链并以 go version -m 验证 github.com/aif-go/ag-core 路径，旧 GitLab 二进制禁止用于脚手架。
- (event) ag-core source finalized per user: GitHub checkout /Users/zhangyong/Downloads/ag-core at 7bc2f4561a9284728cb92b15b9ae9ee760abfa5c; installed GitLab-built toolchain recorded as P0 replacement
- (constraint) GitHub ag-core 当前 commit 7bc2f4561a9284728cb92b15b9ae9ee760abfa5c 仅位于本地分支 feat/agbatch_fault_tolerance_refinement，尚无远端分支包含；只能作为已验证工作快照，不能作为 CI 可恢复锁。
- (decision) ag-core 规范来源改为 https://github.com/aif-go/ag-core.git + module github.com/aif-go/ag-core；本机 checkout 路径仅作证据。进入脚手架/CI 前必须将工作快照发布为远端可达不可变 tag/ref，technology-lock 记录 ref+SHA，并从其重建 aggo、gendb、protoc-gen-go-ag*。
- (event) ship review fixes applied: normative GitHub remote, local SHA marked unrecoverable snapshot, immutable remote ref P0, aggo+gendb+all protoc tools rebuild, source inputs and reproducible verification included
